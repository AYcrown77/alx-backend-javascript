export default function createReportObject(employeesList) {
  const result = {
    allEmployees: employeesList,
    getNumberOfDepartments(dept) {
      returnObject.keys(dept).length;
    },
  };
  return result;
}
